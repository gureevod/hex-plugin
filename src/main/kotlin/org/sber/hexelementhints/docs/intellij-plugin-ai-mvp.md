# IntelliJ Plugin + LangChain4j: MVP

## Обзор

Расширение существующего IntelliJ плагина для интеграции с LLM через LangChain4j.
Авторизация через клиентский сертификат (PKCS12).

---

## MVP Scope

### Функциональность

1. **Настройка подключения** — выбор cert.p12 + пароль
2. **Генерация PageObject** — из HTML файла или выделенного текста

### Что НЕ входит в MVP

- Множественные провайдеры LLM
- История чатов
- RAG/индексация проекта
- Другие генераторы (REST, тесты)

---

## UI/UX Flow

### Первый запуск (сертификат не настроен)

```
┌─────────────────────────────────────────────────────────┐
│  Hex AI Assistant                              [⚙️]     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│     🔐 Требуется настройка подключения                 │
│                                                         │
│     Для работы с AI необходимо указать                 │
│     клиентский сертификат (cert.p12)                   │
│                                                         │
│     ┌─────────────────────────────────────────────┐    │
│     │         [ Настроить подключение ]           │    │
│     └─────────────────────────────────────────────┘    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Диалог настройки сертификата

```
┌─────────────────────────────────────────────────────────┐
│  Настройка подключения к AI                      [X]   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Сертификат (PKCS12):                                  │
│  ┌─────────────────────────────────────────┐ ┌──────┐  │
│  │ /Users/test/ssl/cert.p12                │ │ ...  │  │
│  └─────────────────────────────────────────┘ └──────┘  │
│                                                         │
│  Пароль сертификата:                                   │
│  ┌─────────────────────────────────────────────────┐   │
│  │ ••••••••                                        │   │
│  └─────────────────────────────────────────────────┘   │
│  ☑️ Сохранить пароль в Credential Store               │
│                                                         │
│  API Endpoint (опционально):                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │ https://internal-llm.company.com/v1             │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  Модель:                                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │ gpt-4 ▼                                         │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│           ┌────────────┐  ┌─────────────────┐          │
│           │   Отмена   │  │  Проверить и    │          │
│           │            │  │    сохранить    │          │
│           └────────────┘  └─────────────────────┘      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Кнопка "..."** — открывает нативный `FileChooserDescriptor` для выбора .p12 файла.

### Главное окно (после настройки)

```
┌─────────────────────────────────────────────────────────┐
│  Hex AI Assistant                              [⚙️]     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  🟢 Подключено: gpt-4                                  │
│                                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                         │
│  📄 Генерация PageObject                               │
│  ─────────────────────────────────────────────────────  │
│                                                         │
│  HTML источник:                                        │
│  ○ Текущий файл (login.html)                          │
│  ○ Выделенный текст                                   │
│  ● Выбрать файл...                                    │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ Дополнительные инструкции (опционально):        │   │
│  │                                                  │   │
│  │ Добавь метод для невалидного логина             │   │
│  │                                                  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │              [ Сгенерировать ]                  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                         │
│  📋 Результат:                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ @Page(url = "/login", title = "Login")          │   │
│  │ public class LoginPage extends BasePage {       │   │
│  │     @Element(name = "Email",                    │   │
│  │              css = "[data-testid='email']")     │   │
│  │     public Input email;                         │   │
│  │     ...                                         │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌────────────────┐  ┌────────────────┐                │
│  │  📋 Копировать │  │  💾 Сохранить  │                │
│  └────────────────┘  └────────────────┘                │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                   IntelliJ Plugin                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │              UI Layer                            │   │
│  │  ┌─────────────┐  ┌──────────────────────────┐  │   │
│  │  │ ToolWindow  │  │ SettingsConfigurable     │  │   │
│  │  │ (боковая    │  │ (File > Settings >       │  │   │
│  │  │  панель)    │  │  Tools > Hex AI)         │  │   │
│  │  └──────┬──────┘  └────────────┬─────────────┘  │   │
│  └─────────┼──────────────────────┼────────────────┘   │
│            │                      │                     │
│  ┌─────────▼──────────────────────▼────────────────┐   │
│  │              Service Layer                       │   │
│  │  ┌─────────────────────────────────────────┐    │   │
│  │  │ HexAiService (@Service)                 │    │   │
│  │  │  - generatePageObject(html, context)    │    │   │
│  │  │  - testConnection()                     │    │   │
│  │  └─────────────────┬───────────────────────┘    │   │
│  │                    │                             │   │
│  │  ┌─────────────────▼───────────────────────┐    │   │
│  │  │ LlmClientFactory                        │    │   │
│  │  │  - createClient(settings)               │    │   │
│  │  │  - Конфигурирует SSL/TLS                │    │   │
│  │  └─────────────────┬───────────────────────┘    │   │
│  └────────────────────┼────────────────────────────┘   │
│                       │                                 │
│  ┌────────────────────▼────────────────────────────┐   │
│  │              Settings Layer                      │   │
│  │  ┌─────────────────────────────────────────┐    │   │
│  │  │ HexAiSettings (PersistentStateComponent)│    │   │
│  │  │  - certPath: String                     │    │   │
│  │  │  - apiEndpoint: String                  │    │   │
│  │  │  - modelName: String                    │    │   │
│  │  └─────────────────────────────────────────┘    │   │
│  │  ┌─────────────────────────────────────────┐    │   │
│  │  │ CredentialStore (IntelliJ API)          │    │   │
│  │  │  - Безопасное хранение пароля           │    │   │
│  │  └─────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────┘   │
│                       │                                 │
└───────────────────────┼─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│                   LangChain4j                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │ OpenAiChatModel                                  │   │
│  │  + SSL с клиентским сертификатом                │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## Технические детали

### Хранение настроек

```kotlin
// Application-level settings (путь к серту, endpoint)
@State(
    name = "HexAiSettings",
    storages = [Storage("hex-ai.xml")]
)
class HexAiSettings : PersistentStateComponent<HexAiSettings.State> {
    
    data class State(
        var certPath: String = "",
        var apiEndpoint: String = "https://api.openai.com/v1",
        var modelName: String = "gpt-4",
        var savePassword: Boolean = true
    )
}
```

### Безопасное хранение пароля

```kotlin
// Используем IntelliJ CredentialStore
object PasswordStorage {
    private const val SERVICE_NAME = "HexAiPlugin"
    private const val KEY = "certPassword"
    
    fun savePassword(password: String) {
        PasswordSafe.instance.setPassword(
            CredentialAttributes(SERVICE_NAME, KEY),
            Credentials(KEY, password)
        )
    }
    
    fun getPassword(): String? {
        return PasswordSafe.instance.getPassword(
            CredentialAttributes(SERVICE_NAME, KEY)
        )
    }
}
```

### Выбор файла сертификата

```kotlin
// Нативный диалог выбора файла
fun chooseCertificateFile(project: Project): VirtualFile? {
    val descriptor = FileChooserDescriptorFactory
        .createSingleFileDescriptor()
        .withFileFilter { it.extension == "p12" || it.extension == "pfx" }
        .withTitle("Выберите сертификат")
        .withDescription("Файл клиентского сертификата PKCS12 (.p12, .pfx)")
    
    return FileChooser.chooseFile(descriptor, project, null)
}
```

### Создание LLM клиента

```kotlin
class LlmClientFactory {
    
    fun createClient(settings: HexAiSettings.State, password: String): ChatLanguageModel {
        val sslContext = createSslContext(settings.certPath, password)
        
        val httpClient = HttpClient.newBuilder()
            .sslContext(sslContext)
            .build()
        
        return OpenAiChatModel.builder()
            .baseUrl(settings.apiEndpoint)
            .modelName(settings.modelName)
            // Интеграция с кастомным HTTP клиентом
            .customHttpClient(httpClient)
            .build()
    }
    
    private fun createSslContext(certPath: String, password: String): SSLContext {
        val keyStore = KeyStore.getInstance("PKCS12")
        FileInputStream(certPath).use { fis ->
            keyStore.load(fis, password.toCharArray())
        }
        
        val kmf = KeyManagerFactory.getInstance(KeyManagerFactory.getDefaultAlgorithm())
        kmf.init(keyStore, password.toCharArray())
        
        val tmf = TrustManagerFactory.getInstance(TrustManagerFactory.getDefaultAlgorithm())
        tmf.init(keyStore)
        
        return SSLContext.getInstance("TLS").apply {
            init(kmf.keyManagers, tmf.trustManagers, SecureRandom())
        }
    }
}
```

### Фоновая генерация (не блокировать UI)

```kotlin
class HexAiService(private val project: Project) {
    
    fun generatePageObject(
        html: String,
        additionalInstructions: String?,
        onSuccess: (String) -> Unit,
        onError: (Throwable) -> Unit
    ) {
        // Запуск в background thread с прогресс-баром
        ProgressManager.getInstance().run(object : Task.Backgroundable(
            project, 
            "Генерация PageObject...", 
            true
        ) {
            override fun run(indicator: ProgressIndicator) {
                indicator.isIndeterminate = true
                
                try {
                    val client = getOrCreateClient()
                    val prompt = buildPrompt(html, additionalInstructions)
                    val result = client.generate(prompt)
                    
                    // Возврат в EDT для обновления UI
                    ApplicationManager.getApplication().invokeLater {
                        onSuccess(extractCode(result))
                    }
                } catch (e: Exception) {
                    ApplicationManager.getApplication().invokeLater {
                        onError(e)
                    }
                }
            }
        })
    }
}
```

---

## Структура файлов плагина

```
hints-plugin/
├── src/
│   └── main/
│       ├── kotlin/com/hex/plugin/
│       │   ├── ai/
│       │   │   ├── HexAiService.kt          # Сервис генерации
│       │   │   ├── LlmClientFactory.kt      # Фабрика клиентов
│       │   │   ├── PromptBuilder.kt         # Построение промптов
│       │   │   └── CodeExtractor.kt         # Извлечение кода из ответа
│       │   ├── settings/
│       │   │   ├── HexAiSettings.kt         # Персистентные настройки
│       │   │   ├── HexAiConfigurable.kt     # UI настроек (Settings)
│       │   │   └── PasswordStorage.kt       # Хранение пароля
│       │   ├── ui/
│       │   │   ├── HexAiToolWindow.kt       # Боковая панель
│       │   │   ├── HexAiToolWindowFactory.kt
│       │   │   ├── SetupPanel.kt            # Панель настройки серта
│       │   │   └── GeneratorPanel.kt        # Панель генерации
│       │   └── actions/
│       │       └── GeneratePageObjectAction.kt  # Context menu action
│       └── resources/
│           ├── META-INF/
│           │   └── plugin.xml
│           └── prompts/
│               └── page-object-system.txt
├── build.gradle.kts
└── gradle.properties
```

---

## plugin.xml дополнения

```xml
<idea-plugin>
    <!-- Существующее содержимое -->
    
    <!-- Tool Window -->
    <extensions defaultExtensionNs="com.intellij">
        <toolWindow 
            id="Hex AI" 
            anchor="right" 
            factoryClass="com.hex.plugin.ui.HexAiToolWindowFactory"
            icon="/icons/hex-ai.svg"/>
        
        <!-- Settings page -->
        <applicationConfigurable 
            parentId="tools"
            instance="com.hex.plugin.settings.HexAiConfigurable"
            id="com.hex.plugin.settings.HexAiConfigurable"
            displayName="Hex AI Assistant"/>
        
        <!-- Application service -->
        <applicationService 
            serviceImplementation="com.hex.plugin.settings.HexAiSettings"/>
        
        <!-- Project service -->
        <projectService 
            serviceImplementation="com.hex.plugin.ai.HexAiService"/>
    </extensions>
    
    <!-- Context menu action -->
    <actions>
        <action 
            id="HexAi.GeneratePageObject"
            class="com.hex.plugin.actions.GeneratePageObjectAction"
            text="Generate PageObject"
            description="Generate PageObject from HTML"
            icon="/icons/hex-ai.svg">
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
```

---

## Зависимости (build.gradle.kts)

```kotlin
dependencies {
    // LangChain4j
    implementation("dev.langchain4j:langchain4j:0.35.0")
    implementation("dev.langchain4j:langchain4j-open-ai:0.35.0")
    
    // Если нужен Ollama для локальной разработки
    implementation("dev.langchain4j:langchain4j-ollama:0.35.0")
}
```

---

## Дополнительные идеи (после MVP)

### 1. Context Menu Integration

Правый клик на `.html` файле → "Generate PageObject" — сразу открывает Tool Window с загруженным файлом.

### 2. Preview с подсветкой синтаксиса

Использовать `EditorTextField` с Java highlighting для отображения результата.

### 3. Авто-определение имени класса

Анализировать HTML (title, form id, page structure) для предложения имени класса.

### 4. Сохранение в правильную директорию

Предлагать путь на основе структуры проекта:
```
src/main/java/com/company/pages/LoginPage.java
```

### 5. Notification при успехе

```kotlin
NotificationGroupManager.getInstance()
    .getNotificationGroup("HexAi")
    .createNotification(
        "PageObject создан", 
        "LoginPage.java сохранён в src/main/java/pages/",
        NotificationType.INFORMATION
    )
    .notify(project)
```

### 6. Keyboard Shortcut

```xml
<keyboard-shortcut 
    keymap="$default" 
    first-keystroke="ctrl alt G"/>
```

---

## План реализации

| Этап | Задача | Время |
|------|--------|-------|
| 1 | Настройки + хранение пароля | 2-3 ч |
| 2 | LlmClientFactory с SSL | 2-3 ч |
| 3 | Tool Window UI (настройка) | 2-3 ч |
| 4 | Tool Window UI (генерация) | 3-4 ч |
| 5 | HexAiService + промпты | 2-3 ч |
| 6 | Тестирование и отладка | 2-3 ч |
| **Итого** | | **~15 ч** |

---

## Вопросы для уточнения

1. **Какой endpoint у LLM?** — Полный URL нужен для настройки.

2. **Есть ли несколько моделей на выбор?** — Если да, можно добавить dropdown.

3. **Нужна ли проверка соединения при сохранении настроек?** — "Проверить подключение" кнопка.

4. **Где хранить промпт-шаблоны?** — В ресурсах плагина или дать возможность редактировать?

5. **Нужен ли режим Ollama?** — Для локальной разработки без сертификата.
